services:
  tools-web:
    image: nginx:alpine
    restart: always
    ports:
      - "8080:80"
    volumes:
      - ./webapp:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
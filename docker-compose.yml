services:
  tools-web:
    image: nginx:alpine
    restart: always
    ports:
      - "9999:80"
    volumes:
      - ./webapp:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro